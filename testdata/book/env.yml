desc: Tests to interpolate environment variables.
debug: ${DEBUG:-true}
interval: ${INTERVAL:-5}
vars:
  number: 1
  string: "string"
  object:
    property: "property"
  array:
    -
      property: "property"
