desc: Test using SSHd with host rules (wildcard)
runners:
  sc: ssh://testuser@ssh.example.com:22
hostRules:
  '*.example.com': ${TEST_SSH_HOST_RULE}
steps:
  uname:
    sc:
      command: pwd
    test: current.stdout contains '/home/testuser'
